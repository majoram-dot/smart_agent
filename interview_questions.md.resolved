# SmartAgent 深度面试问题库（100+题）

这份问题库针对真实场景落地与系统重构中的核心决策，旨在通过“剥洋葱”式的层层深入，帮助你验证是否真正吃透了整个项目。

## 一、 架构设计与系统重构 (1-15)
1. 在重构前，项目遇到了什么循环依赖的具体痛点？你是如何定位出哪些模块导致了依赖成环的？
2. 为什么要将协议层 `base/tool.py` 完全抽空业务依赖？如果是为了让别人复用，为什么不直接提供装饰器（Decorator）模式？
3. 项目采用“单向向下依赖”的架构，那如果底层的工具在执行时确实需要获取最顶层的 Agent 运行上下文或临时状态，你是怎么解决数据传递传递的？为什么不用全局变量或事件总线模型？
4. 为什么 `agents/core.py` 作为协调层拥有横向整合服务层（如 memory/rag/tools）的特权？如果将其打散成多个平级的微服务，你们现有的代码需要做多大改动？
5. `deep_research` 被放在了功能扩展层，它是怎么基于现有的 `agents` 层组装起来的？如果将它硬编码到 `core.py` 里会有什么坏处？
6. 在真实的生产环境中，UI 层（Gradio）是怎么拿到 Agent 底层的流式思考步骤输出的？为什么不让 Agent 直接 import 网关回调函数来推流？
7. 如果要将这个项目开源，按照目前的结构，二次开发者最大的心智负担和集成成本会在哪里？
8. 服务层（Tools, Memory, RAG）目前是平行的，如果 RAG 检索时需要请求网关做 Web 检索（WebSearchTool），在严格平行的规定下跨模块调用怎么处理？为什么不重构为树形调用链？
9. `base.tool` 提供了向后兼容的 `run(dict)` 方法，这种为了兼容旧代码而在协议层留下字典解包拦截的设计是否是一种妥协？换作是你，会不会直接强制上游修改传参格式？为什么？
10. 你知道什么是控制反转（IoC）和依赖注入（DI）吗？在这个 Agent 架构里是否有体现？如果没有，为什么不用这套标准的范式？
11. 什么是限界上下文（Bounded Context）？你们在划分 memory 和 RAG 边界时，是如何决定一个上下文到底算长期记忆存储还是文档向量库存储的？
12. 在重构过程中踩过的最大的坑是什么？你怎么通过测试用例保证重构没有破坏原先 Agent 的核心决策树？
13. 系统重构带来的性能提升及开发效率的收益是如何量化的？
14. 如果要将这个单体 Agent 部署为分布式的微智能体架构群，当前代码结构中哪些模块的代码需要推翻重来？
15. 现在的 `TOOL_MAPPING` 是静态注册的，为什么不使用基于类扫描机制的动态插件加载架构？这样不是耦合度更低吗？

## 二、 核心中枢：Agent 运行机制 (16-30)
16. 你们项目同时保留了 `CodeAgent` 和 `ToolCallingAgent`，它们的本质区别是什么？
17. 为什么说生成 Python 代码让 `CodeAgent` 执行，比让传统 Agent 输出 JSON 去调用函数更强大？它在解决什么业务痛点时有绝对优势？
18. 大多数 Agent 在多次 Tool Call 失败后会进入死循环（死锁），你们在 `MultiStepAgent` 内部是通过什么机制（例如 最大重试、报错反射上下文、冷却惩罚）来打破死锁的？为什么要用这套机制而不是直接截断？
19. `CodeAgent` 生成的代码如果引发异常，系统是如何截获并将 traceback 完整反馈给大模型让它执行自我纠错的（Self-Correction）？
20. 你认为让 LLM 每次吐出一段 Python 代码并且在沙盒中解析 `print()` 捕获结果，和通过 JSON 中定义严格的 response_schema 返回数据，哪种容错率更低？为什么你们选了前者？
21. 工具调用的上下文通常会占用大量的 Token，当运行步骤超过 20 步时，你们是怎么裁剪系统 Prompt 和历史轨迹以防止 Token 撑爆的？为什么不用摘要压缩所有的历史对话？
22. 系统如何区分“规划（PlanningStep）”和“行动（ActionStep）”？这两者的流转状态机是怎样的？
23. 如果用户输入的要求是多步骤的（比如：“先去网上搜索苹果股票，然后帮我存到本地，接着发送一封邮件”），框架是怎么保证 Agent 不会中途遗忘或乱序执行的？为什么不用类似 LangGraph 那样的图结构？
24. 在面对复杂任务时，Agent 生成的计划可能全错。现在的系统中有实现基于 Reflexion 的反馈验证（规划执行并反思修改）机制吗？
25. LLM 如果幻觉导致它一直在调用不存在的函数，或者参数一直填错，你的系统容错层最高能支撑几次修正尝试才抛出报错停止？
26. 如果需要 Agent 进行多并发请求（比如同时去 Google 查询五个不同的关键词），目前的调度机制支持将步骤进行并行执行并汇聚结果吗？怎么改能支持？
27. 所谓的高级 Agent 规划能力，很多时候取决于底层 LLM 的推理深度，你们在测试各个大模型时，有发现哪类开源模型在 CodeAgent 上的效果出奇的好或出奇的差吗？具体差在什么环节？
28. 有些用户会通过提示词注入（Prompt Injection）要求 Agent 删掉主机中的文件。系统 prompt 层如何防御这种行为？安全沙箱是必须的吗？
29. 当用户的诉求模棱两可时，Agent 会直接假设一个答案执行，还是会通过某个特殊 Tool 停下来请求人类确认（Human in the loop）？如果现在没做，加上这一步最合理的介入点在哪？
30. 为什么选择复用 HuggingFace 的 smolagents 基座而不是用成熟度更高的 AutoGen 或 LangChain 框架？除了轻量级之外，你们看中它什么？

## 三、 RAG 系统与高级检索 (31-45)
31. `TinyRAG` 虽然轻量，但为什么要自己手搓一份 `VectorStore` (HNSW) 而不是直接使用成熟的 Milvus 或者 Pinecone 等云端向量库？
32. `documents.py` 在处理长文本切片（Chunking）时，有没有考虑文档的语义连贯性？如果是一段有着明显小标题的 Markdown 长文，纯按字数切片的弊端是什么？
33. 你们的高级搜索引入了 `HyDE`（假设性文档嵌入），这种技术的本质是用大模型生成伪答案去匹配。当原始查询词本来就很清晰时，HyDE 引入大模型的幻觉杂质反而会导致召回率下降，你们有做查询路由识别吗？为什么不做？
34. 解释一下系统中的多路“混合检索”（Hybrid Search）。单纯依赖稠密向量（Dense Vector）检索会有什么致命缺陷？
35. `BgeReranker` 重排序模型在这个流水线里扮演什么角色？为什么不直接取向量检索的 Top-5 拿去生成回答，而非要在大批量召回后再做一次计算量巨大的交叉重排？
36. 在 GSSC 流水线（生成-存储-检索-组合）中，对于过期冲突的知识，如果本地存在两个相似但结论相反的文档，系统最终如何让 LLM 不由于上下文矛盾而懵逼？
37. 如何处理涉及图片或表格的多模态文档解析？目前的 RAG 子模块能支持 `MarkItDown` 进行图文混合知识挖掘吗？
38. 你在测试 HNSW 索引时，召回的准确率和索引构建的时间是怎样的关系？有没有遇到内存暴涨的问题？
39. 为什么 7 种不同的 Embedding 模型要被封装在一起？如果要外挂一个新的本地 Embedding（如 m3e-base），改动成本在哪里？
40. 如果我想给检索加上特定的 MetaFilter（比如只搜索 2024 年之后的文档），目前的单纯向量存储和查询机制支持吗？怎么改动最优雅？
41. 当用户的请求是：“对比一下文档 A 和文档 B 中的架构区别”，这属于传统的 RAG 难解的 Multi-hop 查询。你的系统对这类请求的支持好吗？为什么？
42. RAG 系统如果在没有召回任何有价值内容时，框架是怎么阻止“强行胡编乱造”（Hallucination）的？有没有引入置信度阈值短路机制？
43. 如果被向量化的文本需要随时频繁增删改查（CRUD），目前基于本地文件结构的内存态 HNSW 能满足需求吗？这和落地实际场景有什么冲突？
44. 在工业界，有很多通过知识图谱（GraphRAG）来增强向量的技术，你认为它比你们现在的方案优势有多大？在你们项目中接入的可行性高吗？
45. 有些查询属于宽泛搜索（比如：“给我讲讲你们公司的规章制度汇总”），这时候普通的 Top-k RAG 一般会失败，针对全量提要，应该用怎样的策略（例如 Map-Reduce 或者长文直接总结）？你们是怎么设计的？

## 四、 4级长期记忆系统设（Memory） (46-60)
46. 你们对记忆划分了工作、情景、语义、感知共计4类，这是参考了人类认知，但真正落地时，**工作记忆 (Working Memory)** 和系统上下文的 prompt trace（AgentMemory）在职责上有什么重叠和冲突？
47. 为什么需要将单次运行的短期记忆提取后沉淀为 **语义记忆 (Semantic Memory)**？这个提炼总结的时机是在什么节点（异步定时、单次会话结束，还是按 token 长度触发）？为什么这样设计？
48. 你们把 **感知记忆 (Perceptual)** 形容为最近感官缓冲区。在代码层面，它是怎么存活并且淘汰的？用的 FIFO 队列还是 LRU 机制？
49. 带有时间戳的 **情景记忆 (Episodic)** 实际上是一张流水账。如果系统跑了几个月，情景记忆变得极其庞大，再做上下文查询时性能怎么保证？
50. `MemoryManager` 是通过什么机制感知不同子系统触发的事件并将其持久化的？如果是单机版，多线程下的安全锁做得如何？
51. 所有的记忆最终是被向量化存储还是用 SQLite 关系数据库落盘的？为什么选了现有的落盘方式？
52. 当 Agent 回答新问题时，如何判断应该去 `语义记忆` 里读结论，还是去 `工作记忆` 里找上下文？这套路由机制是怎么避免查询风暴的？
53. 如果一个用户过去存储了一条语义记忆（例如：“我喜欢红色”），而在新的对话中他纠正说（“我现在喜欢蓝色了”）。你们如何做到对长期记忆对象的更新或作废（知识修剪），而不是产生两条冲突的记忆？
54. `MemoryTool` 作为包装器给到了 Agent。也就是说，是靠 LLM 主动去调用记忆查找函数的？如果大模型太“懒”，不想调用它，系统岂不是变成了“间歇性失忆”？为什么不做成强制自动前置检索注入的方法？
55. 除了记忆总结外，你们有做重要对话内容的热点提取（Entity Extraction）吗？
56. 不同用户（Session ID / User ID）之间的记忆目前是做到了底层结构隔离的吗？如果用 Redis 应该怎么组织这个 Key-Value 层级？
57. 在大模型推理中，记忆系统如果强行注入过多无关的“关联偏好”，会破坏 LLM 原本强大的泛化回答。遇到过这种“过度拟合记忆”的负面例子吗？
58. 既然有系统自带的 RAG，为什么还要单独做一套长短记忆存储？这两者的边界到底是什么？（比如 RAG 是外部客观知识，Memory 是用户个性主观偏好？）
59. 随着模型上下文窗口不断变大（如 Gemini 1.5 的百万 Token），复杂的记忆分区还像以前那么有必要吗？为什么不直接把过去两周所有的原始日志扔进去让模型自己找？
60. 如果要我评估你们这个四层记忆系统的吞吐能力，你应该抛出哪几个监控指标（比如命中率、提炼延迟等）供我查看？

## 五、 工具体系与控制反转 (61-75)
61. 你们的基类 `AbstractTool` 要求强制实现 `__call__`。为什么还要多写一个 `to_tool_calling_prompt` 的方法，这个方法具体往外输出了什么？
62. 对第三方包（或原生库）进行 Tool 封装时，你是如何进行 JSON Schema 参数推导的（从 Type Hints 提取还是手动写 YAML 注册）？
63. `MethodChecker` 是通过 AST 静态校验拦截风险方法的。如果在实际的动态 Python 中，攻击者用了类似于 `getattr(obj, b'\x6f\x73'.decode())` 之类的混淆方法来调用系统内置库，你的保护层能防住吗？为什么？
64. MCP 客户端（`MCPTool`）这个模块极大地扩展了工具库集，它是基于什么协议交互的（HTTP 还是 Stdio）？为什么现在行业里都在推崇 MCP 作为标准？
65. WebSearchTool 背负着连接外界信息的重任。DuckDuckGo 免费但是极不稳定，你们的工具层是否做了完整的降级容错机制或者多 Provider 兜底机制？
66. 在让 Agent 自主使用 `TerminalTool` 时，如果不小心敲入一个死循环命令（如 `top` 或者一个不断报错重试的 `ping`），系统怎么判定它已经超时或进入僵死状态？
67. 当 `NoteTool` 这类工具向外部写入文件时，如何防止目录遍历攻击（比如 Agent 生成了路径 `../../../../etc/passwd`）？
68. 如果你想引入一个外部需要 OAuth 认证的第三方工具服务接口（比如读用户的 Google Calendar），鉴权令牌应该由 Agent 持有还是工具层封装持有？为什么？
69. 我们发现很多时候 LLM 并不能准确理解复杂工具的 4、5 个参数的具体意义。比起在 Prompt 中堆叠说明，有没有尝试在 Tool 执行失败后直接吐出明确的参数校准引导信息？
70. `FinalAnswerTool` 这个设计非常有意思。为什么 Agent 完成所有推理并准备回应用户时，它必须要把它当做一个“特殊工具”来调用一次，而不是简单地直接输出文本退出循环截断就好？
71. 静态的 Tool 注册是在初始化时确定的。如果某些工具是按需加载甚至是由大模型在使用中即时编写的小脚本动态生成的新 Tool，你们这个系统应该如何动态加载并注册它们？
72. 你知道怎么让 Agent 输出包含媒体文件（比如 `Image` 对象 或 音频）的回调吗？在底层序列化（Serialization）层对这类非 JSON 兼容体是怎么处理的？
73. 工具执行层如果有重度 I/O 操作（比如上传 1GB 视频），框架现在有异步化（`async/await`）支持吗？如果全是同步阻塞，你们单实例的上限会很受限吧？为什么不全改成异步？
74. `ToolCollection` 被用来包装一堆工具，在传给大模型做 Function Calling Schema 时，如果工具总量有 100个，会导致极大的 Token 浪费并且让 LLM 懵圈“注意力分散”。你们怎么解决大规模工具池的寻址分发？
75. 在重构过程中为何特别把 AST 校验单独剥离为 `validation.py`？如果把这些规则硬编码在各自的工具包里会有什么坏处？

## 六、 动态执行与远程沙箱安全 (76-90)
76. `LocalPythonInterpreter` 的存在，意味着用户的输入可以在服务器上执行真实的 Python 逻辑。这个方案在物理落地上面临的安全考核会非常严苛，你们目前的沙箱足够说服你们的网络安全部门吗？为什么？
77. 在白名单拦截机制下，`BASE_BUILTIN_MODULES` 开放了哪些库？为什么严格屏蔽了 `os`, `sys`, `subprocess`？如果用户真的因为业务需要执行命令，怎么处理这种矛盾？
78. `eval` 和 `exec` 有什么本质区别？在这个本地解释器里，是用哪一种执行多行复杂代码甚至类定义的？怎么隔离 `globals()` 命名空间？
79. 如果模型产生了一段有极高 CPU 负担的死循环代码 `while True: pass`，沙箱是如何实现资源（CPU/内存/时间）限制（Quota & Timeout）的防御机制的？
80. 相比于费劲做本地 AST 过滤，后来引入了 E2B Executor、Docker Executor 或 Modal，这些远程容器执行方案。为什么不直接抛弃本地方案全部切到 Docker 里？它们带来了哪些网络与延迟开销？
81. `DockerExecutor` 在热启动（Cold Start）和镜像维持池上的开销怎么控制？你们是一次请求起一个容器用过即焚，还是保持容器池驻留？
82. 沙箱中如果需要访问外部数据库或网络，但我们又不希望它被利用作为肉鸡发起 DDoS，这种精细化的网络出向控制是如何在代码态与系统态配置结合完成的？
83. Agent 在执行多步计算时（第1步生成结果 x，第2步写新的代码用到 x），在这个过程中 Python 沙箱环境（State / Variables）是如何一直维持热上下文跨步保留的？
84. 有些代码报错不是 Python 语法错误，而是数据形态不对（比如 Numpy Index Out of Bounds）。遇到这种运行时错误（Runtime Error），你们如何准确地把上下文喂回给模型而不是返回干巴巴的一行 Exception？
85. WasmExecutor（WebAssembly）在这套机制里被视作未来的黑马。你觉得 WASM 相比于 Docker 在 Agent 沙箱执行中的核心优势体现在什么地方？
86. 沙箱中如果代码输出了大量的日志（比如 `print([1] * 10000000)`），这种日志爆炸会直接打爆 LLM 的上下文输入限制。系统是否有专门的 stdout/stderr 聚合与阶段阻断机制？
87. Python 是一门动态语言，你们通过 AST 拦截代码，它能否防挡类似于动态 `__import__` 黑客技巧的各种变形手法？
88. 对于执行大批量数理计算任务时，`evaluate_python_code` 会在宿主机上产生哪些临时落盘文件吗？你们有相关的垃圾回收清理机制吗？
89. 如果要你在你的本地引擎中再加一道防线限制文件系统（只能读写特定的小黑屋目录），你会利用 Python 的哪个高级特性（比如 重写 Open 函数，或是 chroot）？哪种方案最符合目前的项目现状？
90. 把生成式 AI 用于自动化编程的难免存在幻觉和废话代码，有加入自动格式化和去除无用调用的优化策略再进入沙箱运行吗？为什么不做？

## 七、 多模型适配、调优与基准评测 (91-105)
91. 你们的 LLM 层采用了 `litellm` 作为路由中枢。这种“百家混合”的模型分发方案如果遇到个别模型不支持某种特定参数（比如不支持特定的 Top_P 或 System Prompt 约束位置）如何优雅降级？
92. `TinyRAG` 也有自己手写的 LLM 封装（`chat.py`），它是怎么跟 `LiteLLMModel` 对接共存的？为什么重构时没有把它们彻底揉进同一个 Interface？
93. 不同的模型对于 `CodeAgent` 的反应截然不同。按照你接触的实际情况，相比于使用 OpenAI 的 GPT-4o，在调用开源（如 LLaMA 3 或 Qwen）时，需要如何调整 System Prompt 里的指令语气和样例，它们才能更听话并且更不易偏离核心任务？
94. 引入基于 BFCL 的评估系统是什么目的？AST 匹配器在这个评测基准里是用来解决衡量什么能力的？（是衡量它理解没理解提议，还是衡量它挑对函数没？）
95. 当利用 GAIA 数据集（涉及复杂的跨模态长期复杂推理问题）对这个框架打分时，你们目前这个架构在遇到哪一类问题时容易出现瓶颈及得分低下？
96. 为什么需要针对特定的重构和核心能力建立一套单独解耦的基准代码环境（`benchmarks/`）而不是直接用 `pytest` 集成进持续集成（CI）管线里面去？
97. Agent 处理速度经常非常慢（因为多步调用）。这让它在真实的 C 端产品（如客服或者 Web Assistant 用户反馈界面）难以落地。从工程角度考虑，除了流式输出（Streaming），你有什么更快的并发方案来降低用户的体感延迟（TTFT）？
98. 记录与监控模块 `monitoring/core.py` 中的 `TokenUsage` 是用来做什么的？在实际跑复杂的多步任务时，你的系统目前有监控预警防“天价账单”消耗机制吗？
99. 在 Prompt 工程上，`AgentLogger` 会怎么记录并清洗脱敏这些敏感数据不进入你们的日志系统分析中？
100. 从演示的 `rag_demo.py` 和 `deep_research_demo.py` 的落地上看，大语言模型很容易在复杂的多阶段研究中丧失全局目标（Goal Tracking drift）。如果不使用人机协作兜底，这套系统有没有基于特定 Metric（如目标剩余度）自动触发强制状态回滚拉回（Rollback）的方法？
101. 现阶段如果要将这个项目用作公司的内部企业大智脑核心框架支撑全员的 Bot 开发，在日志追踪与可观测性（Observability，比如 OpenTelemetry）上有何技术规划？
102. 由于目前系统的 `prompt` 大多是文本写死的或模板化写在 yaml 里的。在大型多团队协作项目中，Prompt 其实是业务层核心资产，你们怎么进行 Prompt 的版本控制演进与 A/B 测试实验管理？
103. 对于 `CodeAgent` 生成的代码，有时我们需要保留那些跑起来真正优质的函数形成一种“被缓存的经验工具箱”。这种让系统越用越聪明的自我学习迭代功能，目前的系统应该建在四层记忆系统的哪一层最好？能无缝接上吗？
104. 当系统同时承载 1000 个并行 Agent 任务（包含内存长期存驻的多阶段规划流）时，最先崩盘的地方会是哪一层级的服务？（底层 API 节流、Python 锁瓶颈、沙箱算力耗尽 还是 SQLite 锁死？）怎么证明这个瓶颈并准备解决方案？
105. 最后一个终极问题：作为一个从旧版冗杂代码中走出来的破壁重构者，如果我们用当前这一套“严格单向依赖分层的新型智能体架构”去跟当下业内的主流大一统框架（如 LangChain，LlamaIndex，Microsoft AutoGen）对线宣讲，我们要拿出什么样的“不可替代的代码结构优势与绝对不可碰触的设计红线底蕴”来证明我们做这款框架是有绝对竞争力和落地生命力的？
